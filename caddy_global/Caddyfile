(common) {
	encode gzip zstd
}

(common_tls) {
	{$TLS_CONFIG}
}

https://{$KUNO_SERVER_ADDRESS} {
	import common
	import common_tls

	reverse_proxy caddy-kuno:80  {
		header_up Host {host}
		header_up X-Real-IP {remote_host}
	}
}